{
  "name": "digital-catapult",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/*.test.mjs\"",
    "build:standalone": "vite build --mode standalone && node scripts/build-standalone.mjs"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}